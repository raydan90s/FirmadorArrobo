services:
  firmador:
    build:
      context: .
      dockerfile: Yachasoft.Sri.FacturacionElectronica/Dockerfile
    container_name: firmador-arrobo
    ports:
      - "5000:80"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
